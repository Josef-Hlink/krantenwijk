// Browser-first SPA: all state lives in memory in the client; nothing is
// rendered on a server (there is none — adapter-static + engine API).
export const ssr = false;
export const prerender = false;
