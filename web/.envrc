# web/ is the Node app. Load the shared devShell (node + pnpm) from the repo
# root, but — unlike the root .envrc — don't activate the Python venv. That
# keeps this subtree's PATH and prompt Node-only.
use flake ..
