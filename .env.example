# Copy to .env (gitignored). Loaded by direnv via dotenv_if_exists.

# OpenRouteService key for walking-route optimization. Free key at
# https://openrouteservice.org — leave blank to use the built-in
# nearest-neighbor fallback (approximate straight-line routes, no account).
ORS_API_KEY=

# Service base URLs. HeiGIT splits the public API per service: directions
# under /openrouteservice, Vroom optimization under /vroom. Self-hosting a
# classic ORS instance? Point both at the same base.
ORS_URL=https://api.heigit.org/openrouteservice
ORS_OPTIMIZATION_URL=https://api.heigit.org/vroom/v0

# Per-IP rate limit for the public API (unused until deployment).
RATE_LIMIT=60/minute

# Upper bound on rows accepted per uploaded CSV.
MAX_UPLOAD_ROWS=5000

# Where saved rounds are stored. LEAVE BLANK to store nothing at all: with no
# database configured the /api/rounds endpoints 404 and the engine is entirely
# stateless, which is the promise DESIGN.md's privacy spine makes.
#
# Setting it opts into storage — saved rounds carry addresses and resident
# names so a phone can show them on the round (see rounds.py). Reaching one
# additionally requires an account; there is no sign-up, and accounts are made
# with `krantenwijk useradd`.
#
# On esther this is a unix-socket DSN with no password: the service runs as a
# DynamicUser named krantenwijk and postgres lets it in by peer auth.
#   postgresql:///krantenwijk?host=/run/postgresql
KRANTENWIJK_DATABASE_URL=
